// Forge
//
// Public entry point. The implementation lives across this module
// as it grows; this file just exposes the namespace and a version
// string so consumers have something concrete to depend on while
// the package is under construction.

import Foundation

public enum Forge {
    /// Semantic version of the Forge package.
    public static let version = "0.0.1"
}
